source-file ~/.tmux/settings.conf
source-file ~/.tmux/mappings.conf

# Plugin tmux-resurrect - https://github.com/tmux-plugins/tmux-resurrect
set -g @resurrect-strategy-nvim 'session'
# Enable saving and restoring tmux pane contents
set -g @resurrect-capture-pane-contents 'on'
# Enable restoring another programs - https://github.com/tmux-plugins/tmux-resurrect/blob/master/docs/restoring_programs.md#general-instructions-
set -g @resurrect-processes 'mc'
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
